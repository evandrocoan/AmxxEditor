# Auto detect text files and perform LF normalization
* text=auto

# Force the following filetypes to have unix eols, so Windows does not break them
*.* text eol=lf
* text eol=lf

# Does not break windows files
*.bat text eol=crlf


# Set lf for sublime files
*.sublime-settings -lf
*.sh -lf


